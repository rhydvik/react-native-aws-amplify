// @flow

export type User = {
  id: string;
  name: string;
  email: string;
  phone: string;
};

export type Page = {
  name: string;
  content: string;
  id: string;
  languageCode: string;
};

export type PageObj = {
  name: Object;
};
